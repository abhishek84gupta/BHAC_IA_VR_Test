- name: Network ping from AWX controller
  hosts: localhost
  gather_facts: false
  tasks:
    - name: ICMP ping target
      ansible.builtin.command: ping -c 4 {{ target_hosts }}
      register: ping_result

    - name: Show ping output
      ansible.builtin.debug:
        var: ping_result.stdout_lines