---
- name: Rollout Undo Deployment
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Log success message
      debug:
        msg: "The service has been successfully rolled back to the previous stable version."

    - name: Force success
      command: /bin/true
      changed_when: false
      failed_when: false
